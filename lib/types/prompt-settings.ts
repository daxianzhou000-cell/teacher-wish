export type PromptSettings = {
  systemRole: string;
  lectureRequirements: string;
  exerciseRequirements: string;
  homeworkRequirements: string;
  parentFeedbackRequirements: string;
  outputRequirements: string;
  extraInstructions: string;
  updatedAt: string;
};
